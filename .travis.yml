before_install:
  - yes | sudo add-apt-repository ppa:hansjorg/rust
  - sudo apt-get update
install:
  - sudo apt-get install rust-nightly python-pip
  - sudo pip install cargo-lite
script:
  - make build tests
  - ./bin/tests
notifications:
  email:
    - tophattedcoder@gmail.com